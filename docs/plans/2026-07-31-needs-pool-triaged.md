# 需求池分档 + 拍板执行路线图 · 2026-07-31

> 来源：微信反馈全量梳理（`docs/feedback/2026-07-22_31-wechat-reconcile.md`，7/22–7/31 九天 900 条）。
> 排序依据：真实摩擦频次×强度（D1）· solo 扛得动（D2）· 是不是差异化护城河。编号=对账表消息号。
> **每项执行标准**：后端能力（如需）+ UI + 五门 + 真机走查（R13）。UI 改动先看真实组件（R8），不脑补。

## 🟢 直接做（惯例交互 / 低争议 / 纯体验提升 —— 不需再拍板）

| # | 需求 | 已核实的改动路径 | 状态 |
|---|---|---|---|
| #888 | 项目库列表**双击改名**（不用点进项目） | 后端缺「按 id 读盘→改 name→存回」（现有 `handleRenameProject` 只改**当前打开**的项目、走内存 payload）；`ProjectLibraryPage.tsx:387` 项目名 div 加双击 inline 编辑 + 新 `onRenameProject(id,name)` prop | **下轮第一个做** |
| #52 | 素材库**双击放大预览** | 复用现成 `NodeMediaPreviewDialog`（已有 mediaType/url/title/onClose）；素材项加 onDoubleClick 打开它 | 待做 |

## 🟡 已拍板（用户 2026-07-31 定方向，照此做）

| # | 需求 | 用户拍板 |
|---|---|---|
| #798 | 生成完成自动另存本地 | **默认关，设置里开**（不改现有习惯；接现有「记住另存目录」f096ddac） |
| #839 | 助手对话引用画布节点 | **对话框 @ 语法**（输入框打 @ 弹节点列表选；要做候选浮层 + 匹配，工作量中） |
| #832 | 画布滚轮缩放 | **设置项二选一，默认滚轮=缩放**（comfyui 习惯优先，保留修饰键方案给笔记派）<br>✅ **已完成 2026-08-03**（`8e67aaf2`）：默认档由 PR#56 落地，二选一设置 + 修饰键档 + 空格拖平移由 `docs/plan/2026-08-03-canvas-gesture-scheme-setting.md` 补齐 |

## ⚪ 先不做（低价值 / 投机大盘 / solo 扛不动，D2「广度是敌人」）

- **自定义节点 / ComfyUI 自定义生成面板**（#891/#463）— 大工程且商品化易被抄，非差异化护城河
- **workbuddy MCP / App 内建 skill / 常用角色 pin**（#180/#362/#94）— 单人弱信号 / 探索性
- **任务状态页 / 分镜导出 word·pdf**（#441/#555）— 锦上添花，非刚需

## 🔵 受外部约束、非纯前端能解决（记录，等条件到）

- **长镜头连贯性**（#619）— 受生成模型时长上限，首尾帧接力是现解；今日论文雷达 CinemaTraj/VideoCoCo 有相关思路
- **全能参考加图数**（#327）— 受模型参考上限约束
- **批量生图一次 N 张**（#311）— ×N 变体已覆盖大半
- **软件单独设代理**（#57）— proxy Phase2，中等工程（主进程代理分期见记忆 proxy-support-phases）

## 执行顺序（下轮起，逐个做透）

1. **项目库双击改名**（#888）→ 2. **素材双击预览**（#52）→ 3. **自动另存·默认关**（#798）→ 4. **助手 @ 语法**（#839）→ 5. ~~**滚轮缩放设置项·默认缩放**（#832）~~ ✅ 已完成（2026-08-03，被 PR#56 提前触发故插队做完）

前两个纯体验提升先清；后三个按拍板方向做。每个独立分支、五门 + 走查闭环。
