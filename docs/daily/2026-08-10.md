# 2026-08-10 工作日志

## 做了什么

**UI 层与逻辑层解耦加固**（`docs/plans/2026-08-10-ui-logic-decoupling.md` 已执行）：

1. **#4 方向分离门岗（先立护栏）**
   - 新增 `src/ui/app-shell/ui-business-decoupling.test.ts`，用源码扫描断言：
     ① `src/ui/app-shell/**` 不得 `import` 任何 `workbench` 路径；
     ② 通用外壳不得直接调用业务 store（`useGenerationCanvasStore` / `useWorkbenchStore`）。
   - 先红（正确捕获 NomiAppBar 违规）→ #1 后转绿。

2. **#1 WorkspaceMode 抽离 + NomiAppBar 解耦（杠杆最大）**
   - 新增 `src/config/workspaceMode.ts`（`WORKSPACE_MODES`/`WorkspaceMode`/`isWorkspaceMode`）。
   - `workbenchStore.ts` 从 config re-export，既有引用方零改动。
   - `NomiAppBar.tsx` 移除所有 workbench import，`OnboardingChecklist`/`TaskCenterButton` 改为 props 插槽注入，`selectNodes` 业务动作上抛到组合层 `WorkbenchShell.tsx`。

3. **#2 DTO 转换与业务编排下沉 api 层**
   - `ChipModel`/`ModelChipKind` 业务类型移到 `src/config/modelChip.ts`。
   - `modelCatalogApi.ts` 新增 `loadOnboardingCatalogSnapshot()` / `testRunCustomCall()` / `upsertCustomCallModel()`。
   - `OnboardingDrawer` / `CustomCallEditor` 改消费 api 函数，删掉组件内手写 DTO 映射。

4. **#5 硬编码 system prompt 抽离**
   - 浏览器复刻/风格 prompt 移到 `src/config/prompts/browserPromptExtraction.ts`。
   - 生成区 agent 的「工具+硬约束」静态段移到 `src/config/prompts/generationCanvasAgent.ts`（字节级重建原 prompt，保住 T2 前缀缓存优化）。

**#3（canvasViewportStore 拆分）**：按计划评估结论**暂缓**（消费面广 + 跨 store 同步负担 > 收益）。

## 门岗结果

| 门岗 | 结果 |
|---|---|
| `typecheck` | ✅ |
| `test` | ✅ 4093 passed / 0 failed |
| `lint:ci` | ✅（max-warnings=98） |
| `build` | ✅ |
| `check:tokens` / `check:i18n` | ✅ |
| `check:filesize` | ⚠️ 既有失败：`electron/main.ts` 807 行 > 800（**非本次改动引入**，未处理） |

## 备注

- `check:filesize` 的 `electron/main.ts` 超标是改动前就存在的，与本次解耦无关，留待后续处理。
- 更新了 `docs/README.md` 的「开发者改代码」表（新增通用 UI 解耦指引）。
