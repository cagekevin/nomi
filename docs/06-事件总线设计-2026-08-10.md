# 06 · 事件总线设计（探索 / 2026-08-10）

> 状态：**探索文档，部分落地**（2026-08-10 复核代码 + 落地 1b）。
> - ✅ **已落地 · 改造 A（IPC 常量化）**：`electron/shared/ipcChannels.ts` 已建 `IpcChannels` + `EventChannels` 两张常量表；机制 A 的裸 `webContents.send` 已收编进 `EventChannels`（20+ 处真实使用，后端发 / `preload.ts` 收，均引常量）。§1「现状=4 套碎片」**已过时**。
> - ✅ **已落地 · 发送侧封装（批次 1b，2026-08-10）**：新建 `electron/events/eventBus.ts` —— `EventMap` 类型表（15 事件 payload 类型化）+ `publishBroadcast`（广播）/ `publishTo`（定向）。11 个发送侧文件已从手写 `webContents.send` 迁到统一封装（`assetEvents`/`reviewTrace`/`autoUpdater`/`textStreamIpc`/`agentChatV2Ipc`/`exportJobIpc`/`screenshotHotkey`/`windowCloseConfirmation`/`productionRunDesktopLifecycle`/`main.ts`/`comfyuiProgressSocket`）。已过 tsc。
> - ⏳ **未落地 · 批次 2（preload 桥自动转发）**：`preload.ts` 仍逐个 `ipcRenderer.on` 转发（channel 已引常量 + 返回 unsubscribe，已规范；但非桥自动转发）。**暂缓**——改为泛型 `onEvent` 需同步改渲染层 API，破坏面大、违反最小差异提交，收益低。机制 B 前端 24 处 CustomEvent 未收口。
> - 关联：IPC 解耦计划见 `docs/plans/2026-08-10-decouple-ipc-runtime.md`（改造 A 已完成，事件总线属改造 B/C 范畴）。**读本文时区分「已落地=§1 现状(部分)+eventBus」与「目标=§4」，勿把目标当现状。**
> 配套：本文是 `02-开发上手` §3.5「改一处动多处」的第四根源，也是 `docs/plans/2026-08-10-decouple-ipc-runtime.md`（A: IPC 常量化）的同源问题——两者都源于「跨层通信靠裸字符串/碎片机制」。

---

## 0. 一句话结论（先给判断）

Nomi **没有统一事件总线，但发送侧已收敛**：改造 A 把 channel 收进 `EventChannels` 常量表，批次 1b 又建了 `electron/events/eventBus.ts`（`EventMap` 类型化 payload + `publishBroadcast`/`publishTo`），11 个发送点已统一。剩余：`preload.ts` 仍逐个 `ipcRenderer.on`（非桥自动转发，批次 2 暂缓）；B 类前端 CustomEvent（24 处）未收口；C（状态）/D（流式）本就不动。

正确方向不是「新造一个全局事件总线」（那是反模式 + 违反 P1 加新必删旧），而是 **把 4 套碎片收敛成「受控、分域、类型化」的单一事件真相**，与 IPC 桥同层、共用命名纪律。现状已走到「发送侧统一 + channel 常量化」，缺的只是接收侧桥自动转发（批次 2，暂缓）。

---

## 1. 现状：4 套事件机制全景（都有真实位置）

> 复核（2026-08-10）：机制 A 的裸字符串**已被 `EventChannels` 收编**（改造 A 落地），不再是"裸字符串碎片"；但仍是"后端直接 send + 前端逐个 on"，**没有事件总线**。机制 B/C/D 现状不变。

### 机制 A · 后端 `webContents.send`（channel 已收编进 `EventChannels`）

后端推事件，channel 全部引用 `electron/shared/ipcChannels.ts` 的 `EventChannels` 常量（单一真相），但仍是「遍历窗口直接 send」、无总线封装：

| channel | 位置 | 场景 |
|---|---|---|
| `nomi:assets:updated` | `electron/assets/assetEvents.ts:13` | 素材写入后广播所有窗口刷新 |
| `nomi:window:close-request` | `electron/windowCloseConfirmation.ts:46` | 关闭确认 |
| `nomi:window:maximized` / `canvas:zoom-shortcut` | `preload.ts` 收 | 窗口/画布状态 |
| `nomi:screenshot:captured/denied/failed` | `electron/screenshot/screenshotHotkey.ts:173-220` | 截图结果 |
| `nomi:production-deep-link` | `electron/productionRun/productionRunDesktopLifecycle.ts:32` | 生产深链 |
| `nomi:tasks:comfyui:progress` | `electron/comfyuiProgressSocket.ts:18` | ComfyUI 进度 |
| `nomi:tasks:text:event` | `electron/ai/textStreamIpc.ts:27` | 文本流 |
| `nomi:exports:event` | `electron/export/exportJobIpc.ts:51` | 导出进度 |
| `nomi:agents:chatV2:event` | `electron/ai/agentChatV2Ipc.ts:53` | agent 会话事件 |
| `nomi:review:event` | `electron/review/reviewTrace.ts:38` | 评审结果 |
| `nomi:update:event` | `electron/update/autoUpdater.ts:20` | 更新状态 |

**✅ 已改进**：channel 名不再是散落裸字符串，全部集中在 `EventChannels`（15 个），新增/改名不再靠手工翻多层；后端发/前端收都引常量，编译器可查。

**⏳ 剩余问题**：仍无总线 —— 每个新事件要手写 `BrowserWindow.getAllWindows()` 循环 + `webContents.send`（见 `assetEvents.ts:8-13`）；前端订阅方在 `preload.ts` 里逐个 `ipcRenderer.on(...)`（非桥自动转发，见 §2）；payload 无类型校验。

### 机制 B · 前端内部 `window.dispatchEvent(new CustomEvent(...))`

渲染进程内各模块自己发 DOM CustomEvent，常量各写各的：

| 事件名 | 位置 | 场景 |
|---|---|---|
| `nomi-open-browser` | `src/workbench/WorkbenchShell.tsx:123` 等 3 处 | 打开浏览器画布 |
| `nomi-open-model-catalog` | `src/workbench/ai/NoTextModelRecoveryCard.tsx:57` 等 6 处 | 打开模型目录 |
| `nomi-model-catalog-changed` | `src/workbench/ai/NoTextModelRecoveryCard.tsx:65` | 模型目录变更 |
| `nomi-focus-generation-node` | `src/workbench/sidebar/CategoryTree.tsx:188` | 聚焦画布节点 |
| `MODEL_REFRESH_EVENT` | `src/config/modelCatalogCache.ts:70` | 模型刷新 |
| `DESKTOP_ACTIVE_PROJECT_CHANGED_EVENT` | `src/desktop/activeProject.ts:16` | 当前项目切换 |
| `PROMPT_EXTRACTION_FEEDBACK_EVENT` | `src/ui/browser/prompt/browserPromptExtractionRunner.ts:126` | 提示词抽取反馈 |

共 **24 处** `window.dispatchEvent(new CustomEvent(...))`，事件名有 `nomi:` 前缀的、有裸名的、有常量抽出的——**命名纪律不统一**。

### 机制 C · Zustand store 当事件用

`workbenchStore.ts` / `generationCanvasStore.ts` / `generationQueueStore.ts` 等 **81 处** 用 `subscribe` / `set` 做事件订阅。状态即事件，耦合进 store，无独立事件语义。**这部分不应强行改成总线**——它是「状态」不是「通知」，见 §5 反模式。

### 机制 D · Node `EventEmitter` 流式

`textStreamIpc.ts` / `comfyuiProgressSocket.ts` 用 EventEmitter 做模型流式输出。**仅限流式场景，不通用**，迁到总线时可作为「流式事件」特例保留。

---

## 2. 前端订阅方现状（preload 里逐个 on，channel 已是常量）

后端 A 类事件在前端由 `electron/preload.ts` 逐个 `ipcRenderer.on(...)` 暴露（**channel 已引 `EventChannels` 常量**，非裸字符串）：

```
electron/preload.ts:49   ipcRenderer.on(EventChannels.windowCloseRequest, ...)
electron/preload.ts:54   ipcRenderer.on(EventChannels.windowMaximized, ...)
electron/preload.ts:59   ipcRenderer.on(EventChannels.canvasZoomShortcut, ...)
electron/preload.ts:161  ipcRenderer.on(EventChannels.assetsUpdated, ...)
electron/preload.ts:287+ ipcRenderer.on(EventChannels.screenshotCaptured/Denied/Failed, ...)
electron/preload.ts:324  ipcRenderer.on(EventChannels.exportsEvent, ...)
electron/preload.ts:345  ipcRenderer.on(EventChannels.tasksTextEvent, ...)
... (共 15 个，全部引常量)
```

**✅ 已改进**：channel 名编译器可查、改名安全。

**⏳ 剩余问题**：仍是**逐个 `ipcRenderer.on` + 业务组件再透传**的三层手动对齐（只是 channel 名安全了）；新增事件仍要在 preload 加一行 + 组件加监听，没有"桥自动转发 + `EventMap` 类型化 payload"。§4 目标形态的「preload 桥一次性转发」未落地。

---

## 3. 为什么这是「改一处动多处」的第四根源

把今天探索出的四个根源并列，你会看清它们的同构性：

| # | 根源 | 散落层 | 类型安全 | 本质 |
|---|---|---|---|---|
| 1 | IPC channel 裸字符串 | main / preload / bridge / 前端 四层 | ❌ | 跨层请求靠字符串 |
| 2 | `runtime.ts` 百货层 | 36 文件反向依赖 | ⚠️ 方向乱 | 类型集中但依赖倒挂 |
| 3 | 供应商适配多文件 | catalog/ 131 文件 | ✅ | 诚实代价，**不动** |
| **4** | **事件机制碎片化** | **A 后端 send / B 前端 CustomEvent / C Zustand / D EventEmitter** | **❌** | **跨层通知靠 4 套碎片** |

根因同构：**跨层通信没有单一真相**。请求侧（#1）和通知侧（#4）是同一道题的两面——都该有「类型化、分域、编译器可查」的中心注册表。

> 注：`assetEvents.ts:10` 的注释已经悟到了正确原理——「写入层是唯一咽喉，挂在这里整类导入路径免费获得回流」——但只做了一半：它用 `webContents.send(EventChannels.assetsUpdated, ...)`（遍历所有窗口推送），channel 已收编进 `EventChannels` 常量，但没升级成 `eventBus.publish`，所以发送侧仍是手写循环。

---

## 4. 目标形态：受控、分域、类型化事件总线

### 4.1 设计原则（先定纪律，再定形状）

1. **单一真相**：所有跨层事件名集中在 `eventChannels.ts`（与 IPC 解耦计划里的 `ipcChannels.ts` 同源、同命名纪律 `nomi:<domain>:<name>`；**改造 A 已完成** —— `ipcChannels.ts` 的 `EventChannels` 已是单一真相。剩下的目标是把「集中常量」升级为「总线 + `EventMap` 类型化 payload」。）
2. **分域隔离**：事件按域分区（`assets.*` / `window.*` / `screenshot.*` / `production.*` / `canvas.*` / `tasks.*` / `model.*`），每个域只暴露自己域的事件，禁止跨域发任意事件。
3. **类型化负载**：`EventMap` 类型表，`emit("assets:updated", payload)` 编译器校验 payload 形状；订阅方 `on("assets:updated", cb)` 自动推断。
4. **单向优先**：默认 **后端 → 前端推送**。前端 → 后端一律走 IPC 请求响应（不抢事件通道）。双向与否见 §4.4 取舍表。
5. **fire-and-forget 与请求响应分离**：事件 = 通知（无返回值）；要返回值用 IPC。绝不混用。

### 4.2 形态草图（不写实现，只定接口形状）

```
electron/shared/ipcChannels.ts       // EventChannels 常量表（单一真相）✅ 已落地
electron/events/eventBus.ts          // 后端发布：EventMap + publishBroadcast/publishTo（2026-08-10 落地）
src/desktop/eventBus.ts              // 前端订阅：on/off（经 preload 桥）——未建，批次 2 暂缓
electron/preload.ts                  // 桥自动转发——未做，仍逐个 ipcRenderer.on（批次 2 暂缓）
```

对比现状（改造 A + 批次 1b 已完成）：
- 现在：加一个事件 = 在 `EventChannels` 加一行常量 + `publishBroadcast`/`publishTo` 发送 + `preload.ts` 加一行 `on` + 业务组件监听。发送侧已统一，接收侧仍是逐个 on。
- 目标：加一个事件 = 只在 `EventMap` 加一行类型；`preload` 桥自动转发，业务组件 `on(...)` 编译器补全 payload（批次 2，暂缓）。

### 4.3 与深模块的关系（回应你的核心诉求）

你要的「深模块 = 改哪只动哪」，事件总线是**最该先建的基础设施**：

- 现状：新增一个「素材导入入口」（捕捞/拖拽/上传/agent/MCP），每个入口都要自己 `broadcastAssetsUpdated` 或在某处手写回流 → 漏一个就少刷新（这正是 `assetEvents.ts:10` 的注释想消灭的）。
- 目标：写入层挂一次总线订阅，所有导入入口免费获得回流。**副作用收敛到单一咽喉 = 改一处只动一处**。

### 4.4 关键取舍：要不要双向？（待你拍板）

| 方案 | 说明 | 用户体感收益 | 风险 / 成本 |
|---|---|---|---|
| **单向（后端→前端）** | 只推送；前端→后端一律 IPC | 解决「改一处动多处」推送侧；最安全 | 前端内部 B 类 CustomEvent 碎片仍在（需另立前端内部总线，见下） |
| **双向但禁请求响应** | 前端也能 `publish` 给 main（如画布交互通知），但禁用事件做请求响应 | 统一前端→后端通知通道，少一套机制 | 需约束「事件 vs IPC」边界，否则退化成全局总线 |
| **含前端内部总线** | 把 B 类 DOM CustomEvent 也收进同一 `EventMap` 封装 | 全链路一套纪律，最完整 | 迁移成本最高（24 处 CustomEvent + 命名统一） |

**我的真判断（D5）**：先落 **单向**（后端→前端），它直接消灭最痛的 A 类裸 `webContents.send` 和 preload 22 行碎片，且不与 IPC 抢语义。B 类前端 CustomEvent 是「前端内部通知」，可第二步用轻量 `EventMap` 封装收口（不跨进程，不涉及 preload），不必和后端总线强绑。C/D 不动。

---

## 5. 迁移路径（按风险排序）

| 批次 | 迁什么 | 从 → 到 | 状态 |
|---|---|---|---|
| 1a | A 类 channel 名 | 裸字符串 → `EventChannels` 常量 | ✅ 已完成 |
| 1b | A 类发送侧 | 手写 `BrowserWindow.getAllWindows()` + `send` → `eventBus.publish` + `EventMap` | ✅ 已完成（2026-08-10，`electron/events/eventBus.ts`，11 文件迁移，过 tsc） |
| 2 | `preload.ts` 逐个 `ipcRenderer.on` | 逐个 → 桥自动转发 + `EventMap` 类型化 payload | ⏳ **暂缓**（需改渲染层 API，破坏面大、收益低，见 §7） |
| 3 | B 类前端 CustomEvent（24 处） | 裸 `dispatchEvent` → 前端 `eventBus` 封装（仅前端内） | ⏳ 待做（中风险） |
| — | C 类 Zustand `subscribe` | **不动**（状态非通知） | — |
| — | D 类 EventEmitter 流式 | **不动**（流式特例，可挂总线「流式事件」标签） | — |

**回滚**：每批独立可回滚；channel 常量与 `EventMap` 同 commit 引入，旧裸字符串在同批删除（P1 加新删旧，无并行版）。

---

## 6. 反模式警示（写进纪律，避免新债）

1. ❌ **禁止全局任意事件**：事件必须分域、集中注册，任何人不能发未登记的事件。
2. ❌ **禁止事件里传大对象**：payload 只放 id / 轻量状态，重数据走 IPC 拉取。
3. ❌ **禁止用事件做请求响应**：要返回值 = IPC；事件 = 通知。
4. ❌ **禁止为「文件少」合并 catalog 供应商适配**（重申 P1）：那是声明驱动多供应商的诚实代价。
5. ❌ **禁止新造第二套事件机制**：新增跨层通知必须走 `eventBus`，旧机制在迁移批次里删除。

---

## 7. 落地取向（现状：1b 已完成，2 暂缓）

**已完成（2026-08-10）**：批次 1b —— `electron/events/eventBus.ts`（`EventMap` 类型化 payload + `publishBroadcast`/`publishTo`），11 个发送侧文件从手写 `webContents.send` 迁到统一封装，过 tsc。新增跨层通知现在：`EventChannels` 加常量 + `publishBroadcast`/`publishTo` 发送，发送侧不再手写窗口循环。

**暂缓（批次 2 · preload 桥自动转发）**：`preload.ts` 仍逐个 `ipcRenderer.on`（channel 已引常量 + 返回 unsubscribe，已规范）。**为何暂缓**：改为泛型 `onEvent` 需同步改渲染层 API 调用面（破坏面大、违反最小差异提交），收益仅「减少 preload 样板」，当前样板已最小化。**将来若要做**：`EventMap` + `preload` 桥一次性转发 + 渲染层 `on(...)` 编译器补全，作为独立一轮，不与已完成的 1b 混提。

**可选（批次 3）**：B 类前端 CustomEvent（24 处）用轻量 `EventMap` 封装收口（纯前端内部、不跨进程，收益低）。

**不动**：C 类 Zustand `subscribe`（状态非通知）、D 类 EventEmitter 流式（流式特例）。

**是否合并**：与 `docs/plans/2026-08-10-decouple-ipc-runtime.md` 保持同源（都收敛到 `EventChannels` 单一真相），但不合并成一份文档——事件总线是改造 B/C 范畴，各自成文。

> 取舍（§4.4）已默认**单向**（后端→前端；前端→后端走 IPC），符合「事件=通知、要返回值用 IPC」纪律。
