# src/styles

全局样式与动画（少数全局 CSS，工程纪律要求全局 CSS 只减不增）。

- `globals.css` / `index.css`：全局基础样式。
- `animations.css`：动画关键帧。
- `vendor-overrides.css`：第三方库样式覆盖。

> 设计 token 与组件级样式见 `src/design/` 与 `src/theme/`。
