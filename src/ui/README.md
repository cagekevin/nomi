# src/ui

通用 UI 组件与外壳。

- `app-shell/`：应用外壳（顶栏/布局/窗口控制）。
- `browser/`：内嵌浏览器 / 文档浏览相关组件。
- `onboarding/`：新手引导 UI。
- `theme/`：主题相关 UI 片段。
- `ErrorBoundary.tsx`、`toast.tsx`、`switch.tsx`、`feedbackLayer.ts`、`chunkBoundary.tsx`：通用反馈/边界/分包组件。
- `*.test.ts`：对应单测。

> 设计系统级组件（按钮、表单、对话框等）在 `src/design/`。
