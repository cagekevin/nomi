# src/utils

无业务依赖的纯工具函数。

- `cn.ts`：className 合并工具。
- `appRoutes.ts` / `routes.ts`：前端路由定义。
- `logger.ts`：前端日志。
- `orientation.ts`：方向/尺寸辅助。
- `showInfoToast.ts` / `showUndoToast.ts`：toast 触发封装。
- `startupDiagnostics.ts`：启动期诊断。
- `videoGenerationSpec.ts`：视频生成规格辅助。
