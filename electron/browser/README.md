# electron/browser

内嵌浏览器 / 文档浏览能力（在窗口内展示第三方页面、捕获截图等）。

- `chrome/`：Chromium 层集成。
- `core/`：浏览器核心逻辑。
- `media/`：浏览器内媒体处理。
- `overlay/`：覆盖层 UI 桥接。
- `settings/`：浏览器相关设置。
- `captureNaming.ts`：截图/捕获命名规则。

> 前端 UI 侧对应 `src/ui/browser/`。
