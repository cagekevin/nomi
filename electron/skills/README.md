# electron/skills

技能（skill）系统：内置技能、清单、包与编排。

- `skillStore.ts` / `builtinSkills.ts`：技能存储与内置技能。
- `skillManifestSchema.ts` / `skillPackage.ts` / `skillCapability.ts`：清单 schema、包、能力。
- `playbookOrchestrator.ts`：playbook 编排器。
- `skillIpc.ts`：技能 IPC。
